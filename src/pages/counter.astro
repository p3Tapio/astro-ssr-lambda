---
import MainLayout from "../layout/main/main.astro";
import CounterComponent from "../components/Counter/Counter";

const title = "Counter";
---

<MainLayout h1={title} title={title}>
  <h4>Click the buttons to see magic happen ✨</h4>
  <p>A test for React components in the build ...</p>
  <CounterComponent client:load />
</MainLayout>
